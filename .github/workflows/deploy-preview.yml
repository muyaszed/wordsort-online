name: Deploy Preview

on:
  pull_request:
    branches: [main]

jobs:
  deploy-preview:
    name: Deploy Preview
    runs-on: ubuntu-latest
    # Vercel preview deployment — wired up in MUY-47
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Vercel (preview)
        run: echo "Vercel preview deploy — configure in MUY-47"
