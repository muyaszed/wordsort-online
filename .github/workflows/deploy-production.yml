name: Deploy Production

on:
  push:
    branches: [main]

jobs:
  deploy-production:
    name: Deploy Production
    runs-on: ubuntu-latest
    needs: []
    # Wired up in MUY-47 (Vercel) after CI passes
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Vercel (production)
        run: echo "Vercel production deploy — configure in MUY-47"
