[phases.install]
# Install all deps including devDependencies so tsx is available at runtime
cmds = ["NODE_ENV=development pnpm install --frozen-lockfile"]

[phases.build]
# No compilation needed — tsx runs TypeScript source directly at startup
cmds = ["echo 'No build step — tsx handles TypeScript at runtime'"]
